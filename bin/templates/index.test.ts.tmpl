import { describe, it } from 'node:test';
import { strict as assert } from 'node:assert';
import * as <%= exports %> from './index.js';


describe('<%= name %>', () => {
	it('has tests', () => {
		assert(<%= exports %>);
		assert.equal(<%= exports %>.greet(), 'hello world!');
	});
});

